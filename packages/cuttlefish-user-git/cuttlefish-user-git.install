post_install() {
    cat <<'EOF'
:: cuttlefish-user installed.

   Start the web operator:

       sudo systemctl enable --now cuttlefish-operator.service

   It then serves the device UI on http://localhost:1080/
EOF
}

post_upgrade() {
    post_install
}
